async def main(message, message_string, client):
    await client.send_message(message.channel, 'Currently not implemented, but will shortly act as the '
                                               'r/dailyprogramming "portal" command')

    return None
